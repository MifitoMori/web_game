{
  "name": "project",
  "private": true,
  "scripts": {
    "dev": "concurrently -n frontend,backend -c blue,green \"npm --prefix web_game run dev\" \"npm --prefix game-backend run start:dev\"",
    "dev:frontend": "npm --prefix web_game run dev",
    "dev:backend": "npm --prefix game-backend run start:dev",
    "build": "npm --prefix game-backend run build && npm --prefix web_game run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
